# Stadia Maps API key — used for the "Bright" (Stadia) basemap tiles on mobile.
# Get one at https://client.stadiamaps.com/ (Manage Properties → API Keys).
# Copy this file to .env and fill in your key. .env is gitignored.
EXPO_PUBLIC_STADIA_API_KEY=
